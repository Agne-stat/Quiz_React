import React from 'react'
import QuizComp from '../components/QuizComp'

function Quiz() {
    return (
        <main>
            <QuizComp themeName={'HTML and CSS'}/>
        </main>
    )
}

export default Quiz
